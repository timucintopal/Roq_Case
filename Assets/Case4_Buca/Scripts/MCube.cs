using UnityEngine;

public class MCube : MonoBehaviour
{
    public static MCube Instance { get; private set; }

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
